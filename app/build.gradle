apply plugin: 'android'

android {
    compileSdkVersion 19
    buildToolsVersion "19.0.1"

    defaultConfig {
        minSdkVersion 8
        targetSdkVersion 19
        versionCode 2
        versionName "2.0"
    }

    signingConfigs {
//        debug {
//            storeFile file("debug.keystore")
//        }

        release {
            storeFile file("keyJogoJOEAndroid.jks")
            storePassword "asusCefet4\$"
            keyAlias "chave google play JOE"
            keyPassword "asusCefetJoe4\$"
        }
    }

    buildTypes {
        debug {
            debuggable true
            jniDebugBuild true
//            signingConfig signingConfigs.debug
        }

        release {
            debuggable false
            runProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
            signingConfig signingConfigs.release
        }
    }
}

dependencies {
    compile 'com.android.support:appcompat-v7:19.+'
    compile fileTree(dir: 'libs', include: ['*.jar'])
}
